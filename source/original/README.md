# Original Source Files

This directory mirrors the original Java source files uploaded for the coursework project, without modifying their contents.

- `Main.java` — Query A / experimental source history as uploaded.
- `MainB.java` — Query B / experimental source history as uploaded.

The original copies are intentionally also preserved at the repository root. This folder exists only to make the repository easier to navigate while keeping the uploaded files untouched.

> Note: these files contain multiple commented implementation iterations. They are preserved as project history rather than rewritten into a cleaned production source tree.
